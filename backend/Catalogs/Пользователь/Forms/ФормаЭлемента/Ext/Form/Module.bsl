
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Объект.ЮрФизЛицо = Перечисления.ЮрФизЛицо.ФизЛицо Тогда 
		ЭтаФорма.Элементы.Наименование.Заголовок = "ФИО пользователя";	
	Иначе 
		ЭтаФорма.Элементы.Наименование.Заголовок = "Наименование организации";	
	КонецЕсли;
КонецПроцедуры
